name: cockpit-container-update
on:
  schedule:
    - cron: '0 4 * * 1'
  # can be run manually on https://github.com/cockpit-project/cockpit/actions
  workflow_dispatch:
jobs:
  cockpit-container-update:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/cockpit-project/tasks
      options: --user root
    permissions:
      pull-requests: write
      contents: write
    steps:
      - name: Set up configuration and secrets
        run: |
          printf '[user]\n\tname = Cockpit Project\n\temail=cockpituous@gmail.com\n' > ~/.gitconfig
          mkdir ~/.config
          echo '${{ secrets.GITHUB_TOKEN }}' > ~/.config/github-token

      - name: Clone repository
        uses: actions/checkout@v7

      - name: Run cockpit-container-update
        run: |
          ./test/common/make-bots
          COCKPIT_BOTS_REF=update-container-workflow bots/cockpit-container-update containers/ws/Containerfile
